//! stage:run stdlib:minimal

/// Returns `x * 2`.
fun double(x: Int) -> Int {
  return x + x
}

public fun main() {
  print(double(Int.zero()))
  print(double(Int.one()))
  print(double(Int.one() + Int.one()))
}
