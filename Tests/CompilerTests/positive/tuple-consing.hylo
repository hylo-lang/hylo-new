//! stage:typing no-std

struct A { public memberwise init }
struct B { public memberwise init }

fun check<T, U>(_ whole: {T, ...U}, _ tail: U) {}

public fun main() {
  check(whole: (B(),), tail: ())
  check(whole: (B(), A()), tail: (A(),))
}
