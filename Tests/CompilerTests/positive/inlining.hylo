//! stage:lowering

@inline(never)
fun one() -> Int32 { 1 }

@inline
fun two(c: Bool) -> Int32 {
  if c { 2 } else { one() }
}

public fun main() -> Int32 {
  // `two` is inlined but `one` isn't.
  two(true)
}
