// Generated by ../../../generator.swift.

/// An unsigned 8-bit integer value.
@_symbol("UInt8")
public struct UInt8 is Deinitializable {

  /// The raw value of this instance.
  internal var value: Builtin.i8

  /// Creates an instance with its raw value.
  internal memberwise init

  /// Creates an instance equal to `false`.
  public init() {
    &self.value = Builtin.zeroinitializer_i8()
  }


}

public given UInt8 is ExpressibleByIntegerLiteral { /* built-in */ }

public given UInt8 is Movable {}

public given UInt8 is Equatable {

  /// Returns `true` if `self` is equal to `other`.
  public fun infix== (other: Self) -> Bool {
    .new(value: Builtin.icmp_eq_i8(self.value, other.value))
  }

  /// Returns `true` if `self` is not equal to `other`.
  public fun infix!= (other: Self) -> Bool {
    .new(value: Builtin.icmp_ne_i8(self.value, other.value))
  }

}

public given UInt8 is Comparable {

  /// Returns `true` if `self` is less than `other`.
  public fun infix< (other: Self) -> Bool {
    .new(value: Builtin.icmp_ult_i8(self.value, other.value))
  }

  /// Returns `true` if `self` is less than or equal to `other`.
  public fun infix<= (other: Self) -> Bool {
    .new(value: Builtin.icmp_ule_i8(self.value, other.value))
  }

  /// Returns `true` if `self` is greater than `other`.
  public fun infix> (other: Self) -> Bool {
    .new(value: Builtin.icmp_ugt_i8(self.value, other.value))
  }

  /// Returns `true` if `self` is greater than or equal to `other`.
  public fun infix>= (_ other: Self) -> Bool {
    .new(value: Builtin.icmp_uge_i8(self.value, other.value))
  }

}

public given UInt8 is AdditiveArithmetic {

  /// Returns `self` added to `other`.
  fun infix+ (other: Self) -> Self {
    let (result, overflow) = Builtin.uadd_with_overflow_i8(self.value, other.value)
    if Bool(value: overflow) {
      fatal_error()
    } else {
      return .new(value: result)
    }
  }

  /// Returns `other` subtracted from `self`.
  fun infix- (other: Self) -> Self {
    let (result, overflow) = Builtin.usub_with_overflow_i8(self.value, other.value)
    if Bool(value: overflow) {
      fatal_error()
    } else {
      return .new(value: result)
    }
  }

  /// Returns `0`.
  static fun zero() -> Self {
    .new()
  }

}

public given UInt8 is Numeric {

  /// Returns `self` multiplied by `other`.
  fun infix* (other: Self) -> Self {
    let (result, overflow) = Builtin.umul_with_overflow_i8(self.value, other.value)
    if Bool(value: overflow) {
      fatal_error()
    } else {
      return .new(value: result)
    }
  }

}